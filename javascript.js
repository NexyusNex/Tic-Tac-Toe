const gameBoard = (() => {
  let gameBoard = ["x", "o", "o", "x", "o", "x", "o", "o", "x"];
})();

const Player = (player) => {
  const name = player;
};

const temp = gameBoard();

function Display() {
  for (let i = 0; i < 9; i++) {}
}
